[package]
name = "kdk-entropy"
version = "0.0.1"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Origin-tagged entropy primitives for KDK"

[dependencies]
kdk-zeroize = { path = "../kdk-zeroize" }
sha2 = { version = "0.10", default-features = false }
