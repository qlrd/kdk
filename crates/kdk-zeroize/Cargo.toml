[package]
name = "kdk-zeroize"
version = "0.0.1"
edition.workspace = true
license.workspace = true
description = "Sensitive-byte primitives for KDK"

[dev-dependencies]
zeroize = { version = "1", features = ["derive"] }
